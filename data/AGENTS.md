# AGENTS — data/

- `claim_ledger.yaml`: when you change a quantitative fact (page count, trim,
  figure count, coverage), update its claim here and point `artifact_path` at the
  artifact that proves it.
- Keep `source_tier` honest: `project_source` (from code/config), `reference_claim`
  (external homage fact), etc.
- This is not where edition content lives — that is `../content/`.
