# data/

Project data and the claim ledger.

- `claim_ledger.yaml` — every quantitative claim this project makes (page count,
  trim dimensions, figure count, coverage), each tied to the artifact that
  substantiates it, so a reviewer can trace each number to its source.

The newspaper's *content* is not here — it lives as YAML under `../content/`.
