# AGENTS — manuscript/

This is the **descriptive manuscript** about the engine, rendered by Stage-03.
It is *not* the newspaper. Editing rules:

- Keep it about the engine/architecture/reproducibility — not about Crescent City.
- One H1 per `NN_name.md` file; keep the numbered ordering.
- Add citations to `references.bib` and reference with `[@key]`.
- To change the *newspaper*, edit `../content/`, never this directory.
