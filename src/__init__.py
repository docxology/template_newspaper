"""template_newspaper — a data-driven newspaper layout engine.

The engine itself lives in the :mod:`newspaper` package (``src/newspaper/``).
This module marks ``src/`` as a package for the canonical exemplar layout; tests
and scripts put ``src/`` on the path and ``import newspaper``.
"""
